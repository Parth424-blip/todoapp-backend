{
  "name": "chapter1",
  "version": "1.0.0",
  "description": "A full stack todo application that uses a NodeJS backend, a SQLlite database and JWT Authentications.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "node --env-file=.env",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
